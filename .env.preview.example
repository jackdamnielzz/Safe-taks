# PREVIEW ENVIRONMENT VARIABLES SETUP
# Copy this file to .env.preview and replace placeholder values with actual credentials
# This file is used for preview deployments of feature branches

# =============================================================================
# FIREBASE CONFIGURATION (Preview)
# =============================================================================
# Use a separate Firebase project for preview environments to avoid data conflicts
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyB...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-preview-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-preview-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-preview-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef123456
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Server-side Firebase credentials (keep secret)
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-preview-project-id"...}

# =============================================================================
# ENVIRONMENT CONFIGURATION (Preview)
# =============================================================================
# Environment indicator for preview deployments
NEXT_PUBLIC_ENVIRONMENT=preview
NEXT_PUBLIC_VERCEL_ENV=preview

# =============================================================================
# SENTRY CONFIGURATION (Preview)
# =============================================================================
# Use preview-specific Sentry project to separate preview errors from production
NEXT_PUBLIC_SENTRY_DSN=https://your-preview-sentry-dsn@sentry.io/project-id
SENTRY_ENVIRONMENT=preview

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Create a separate Firebase project for preview environments
# 2. Copy this file to .env.preview
# 3. Replace all placeholder values with actual preview credentials
# 4. Deploy to preview: ./scripts/deploy-preview.sh
#
# Preview environments use:
# - Separate Firebase project (isolated data)
# - Preview-specific Sentry project (isolated error tracking)
# - Same production features but with isolated data
#
# =============================================================================
# SECURITY NOTES
# =============================================================================
# - Preview environments use test/demo data only
# - Never use production credentials in preview
# - Preview deployments auto-update with branch changes
# - Preview URLs: https://branch-name-safe-taks.vercel.app